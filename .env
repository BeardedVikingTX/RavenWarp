# ============================================================
#  RavenWarp :: .env
# ------------------------------------------------------------
#  NEVER commit real values in this file to the public GitHub
#  repo. Add ".env" to .gitignore immediately. Access is ALSO
#  blocked at the web server level via .htaccess (belt &
#  suspenders) — but .gitignore is the line that actually keeps
#  it off GitHub, so don't skip that step.
# ============================================================

# --- Database (fill in after creating the new DB + user in cPanel) ---
RW_DB_HOST=127.0.0.1
RW_DB_NAME=beardedviking_ravenwarp
RW_DB_USER=beardedviking_claude
RW_DB_PASS=RbXqyOCrwY!CtqRYI2

# --- Encryption (freshly generated — safe to use as-is) ---
RW_ENCRYPTION_KEY=zHceQHj3V+dLB65vAPf0n/Dhk6i1MT9IfaRGJd7WiEc=
RW_HASH_PEPPER=b27600fdc6122e5f40b74ac9765848252a025d327c1a9d2e98d7ff79380bcbff

# --- Mail ---
RW_MAIL_FROM=no-reply@beardedviking.org

# --- Reserved for future features — leave blank until the feature
#     that consumes it actually exists. An unused key is pure risk. ---
RW_STRIPE_SECRET_KEY=
RW_AI_CHAT_API_KEY=